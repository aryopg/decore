defaults:
  - base_decoder_config

name: ActivationDecoding
method: ActivationDecoding
configs:
  post_softmax: True
  dola_layers: high
  alpha: 0.5
  decoding_strategy: entropy
  decoding_mode: activation
  info_layer: 24